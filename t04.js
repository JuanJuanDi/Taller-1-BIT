//Incrementar la variable edad en 1 utilizando el operador de incremento (++) y luego imprimir el nuevo valor.


// Declarar una variable para la edad
let edad = 23;

// Incrementar la variable edad en 1 utilizando el operador de incremento ++
edad++;

// Imprimir el nuevo valor de la edad
console.log("La nueva edad es: " + edad);